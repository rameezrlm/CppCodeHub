#include <iostream>  // Include the input/output stream library

using namespace std; // Using the standard namespace

int main()
{
	// Displaying a numbered message with a line break
	cout << 1 << " C++ Program" << endl; // Insertion operator (<<) to display text, endl adds a newline

	// Providing information about C++'s origin
	cout << "\tC++ was Introduced By Bjarne Strostrup\n";
	// Escape sequences: \t for a tab, \n for a newline. 
	// Insertion operator (<<) to display the text
}
